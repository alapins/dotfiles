export ZDOTDIR="$HOME/.config/zsh"   

. "$HOME/.cargo/env"

# Feed gh CLI token to the GitHub MCP server (Claude Code)
export GITHUB_PERSONAL_ACCESS_TOKEN="$(/home/linuxbrew/.linuxbrew/bin/gh auth token 2>/dev/null)"
