#!/usr/bin/env bash
# Example local install script for `via: local-script`.
# Replace with real install logic; keep it idempotent (check-before-install).
set -euo pipefail
echo "example-local: nothing to do (placeholder)"
